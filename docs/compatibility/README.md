# Compatibility lab

Fixtures are provenance-first.

Each fixture should record the historical implementation, exact version or commit, input and options, observed output, expected Diffloom output, and mismatch classification.

The committed 0.1.0 fixtures cover basic replacement and Unicode BMP behavior. Expanded historical corpora can be added without changing the native contract.
